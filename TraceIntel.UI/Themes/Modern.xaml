<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:system="clr-namespace:System;assembly=mscorlib">
  <!-- Color Palette -->
  <Color x:Key="BackgroundDark">#1E1E2E</Color>
  <Color x:Key="SurfaceDark">#242433</Color>
  <Color x:Key="PrimaryAccent">#6C5CE7</Color>
  <Color x:Key="SecondaryAccent">#7C6CF7</Color>
  <Color x:Key="SuccessGreen">#27AE60</Color>
  <Color x:Key="ErrorRed">#E74C3C</Color>
  <Color x:Key="ForegroundPrimary">#E0E0E0</Color>
  <Color x:Key="ForegroundSecondary">#A8A8C8</Color>

  <!-- SolidColorBrushes -->
  <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundDark}" />
  <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceDark}" />
  <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryAccent}" />
  <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryAccent}" />
  <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessGreen}" />
  <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorRed}" />
  <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource ForegroundPrimary}" />
  <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource SurfaceDark}" />
  <SolidColorBrush x:Key="SecondaryForegroundBrush" Color="{StaticResource ForegroundSecondary}" />

  <!-- Font Family -->
  <FontFamily x:Key="DefaultFontFamily">Inter, Segoe UI, Arial, sans-serif</FontFamily>

  <!-- Gradient Brushes -->
  <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="#6C5CE7" Offset="0" />
    <GradientStop Color="#7C6CF7" Offset="1" />
  </LinearGradientBrush>

  <!-- Shimmer Animation for Skeletons -->
  <Storyboard x:Key="ShimmerStoryboard" RepeatBehavior="Forever">
    <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)"
                     From="0.3" To="0.9" Duration="0:0:1.2" AutoReverse="True" />
  </Storyboard>

  <!-- Fade In Animation -->
  <Storyboard x:Key="FadeInStoryboard" >
    <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)"
                     From="0" To="1" Duration="0:0:0.3" />
  </Storyboard>

  <!-- Slide In From Left -->
  <Storyboard x:Key="SlideInFromLeftStoryboard" >
    <ThicknessAnimation Storyboard.TargetProperty="Margin"
                        From="-30,0,30,0" To="0" Duration="0:0:0.25"
                        DecelerationRatio="0.7" />
    <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)"
                     From="0" To="1" Duration="0:0:0.25" />
  </Storyboard>

  <!-- Base TextBlock Style -->
  <Style TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
    <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
    <Setter Property="FontSize" Value="14" />
  </Style>

  <!-- Base Button Style -->
  <Style TargetType="Button" x:Key="BaseButton">
    <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
    <Setter Property="Foreground" Value="White" />
    <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="Padding" Value="16,8" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="{StaticResource SecondaryBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Background" Value="#4A4A5A" />
              <Setter Property="Foreground" Value="#808080" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Success Button (derived) -->
  <Style TargetType="Button" BasedOn="{StaticResource BaseButton}" x:Key="SuccessButton">
    <Setter Property="Background" Value="{StaticResource SuccessBrush}" />
  </Style>

  <!-- Danger Button (derived) -->
  <Style TargetType="Button" BasedOn="{StaticResource BaseButton}" x:Key="DangerButton">
    <Setter Property="Background" Value="{StaticResource ErrorBrush}" />
  </Style>

  <!-- TextBox Style -->
  <Style TargetType="TextBox">
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="Padding" Value="10" />
    <Setter Property="CaretBrush" Value="{StaticResource PrimaryBrush}" />
    <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
    <Setter Property="FontSize" Value="14" />
  </Style>

  <!-- DataGrid Base Style -->
  <Style TargetType="DataGrid" x:Key="BaseDataGrid">
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="GridLinesVisibility" Value="None" />
    <Setter Property="HeadersVisibility" Value="Column" />
    <Setter Property="RowBackground" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="AlternatingRowBackground" Value="#2D2D3A" />
  </Style>

  <!-- ProgressBar Style -->
  <Style TargetType="ProgressBar" x:Key="BaseProgressBar">
    <Setter Property="Height" Value="30" />
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
    <Setter Property="BorderThickness" Value="2" />
  </Style>

  <!-- Card Border Style -->
  <Style x:Key="CardBorder" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
    <Setter Property="CornerRadius" Value="8" />
    <Setter Property="Padding" Value="12" />
    <Setter Property="Margin" Value="6" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Effect">
      <Setter.Value>
        <DropShadowEffect Color="#000000" BlurRadius="12" Opacity="0.4" Direction="270" />
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Skeleton Rectangle Style -->
  <Style x:Key="SkeletonRectangle" TargetType="Border">
    <Setter Property="Background" Value="#3A3A4A" />
    <Setter Property="CornerRadius" Value="4" />
    <Setter Property="Height" Value="20" />
    <Setter Property="Opacity" Value="0.6" />



  </Style>

</ResourceDictionary>
